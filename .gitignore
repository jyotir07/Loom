# Secrets / local config
.env
.env.local

# Python bytecode / caches
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/

# Virtual environments
venv/
.venv/

# Editors / IDEs
.idea/
.vscode/
*.swp

# Python build artifacts
build/
# dist/
*.egg-info/
*.egg

# Test coverage
.coverage
coverage.xml
htmlcov/

# Runtime databases (SQLiteSink event store)
*.db
*.db-wal
*.db-shm
loom_events.db

# NOTE: site/ is intentionally NOT ignored.
# It holds the Next.js source app. Vercel builds it with Root Directory = site
# (Framework = Next.js). Build artifacts (.next/, node_modules/) are ignored by
# site/.gitignore.

# Node
node_modules/

# OS files
.DS_Store
Thumbs.db

# Project notes (not for VCS)
docs/roadmap.md
PROJECT_CONTEXT.txt
loom_v2.md
landing_fixes.txt
loom_v2_changes.txt
need-checking.txt